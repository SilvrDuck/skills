---
name: x
description: Use when the user types `/x` or references the "press X to doubt" meme. Alias for the `/doubt` skill — same behavior: the user is pushing back on something you just did or claimed, and your job is to re-examine it with fresh skepticism, not to capitulate and not to defend. Activate this whenever `/x`, "press x", or the doubt meme is referenced.
argument-hint: "[hunch — optional]"
---

# x — alias for `/doubt`

`/x` is a meme alias for `/doubt`. Behavior is defined by the doubt skill, injected below:

@skills/doubt/SKILL.md
